[package]
name = "cortex-debug-zed"
version = "0.1.0"
edition = "2024"
license = "MIT"
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.6.0"

[build-dependencies]
# build.rs needs only standard library (fs, path)
